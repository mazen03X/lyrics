add record timestamps for auto play

add upload timestamps


add complex colors edting for fonts

like palettes

add animation

add offset x and whole mesh moving



settings for moving meshes


add record data which could be array of objects that hold the colors, fontsize, animation, timestamps
